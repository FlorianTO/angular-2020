import { SwitchHeurePipe } from './switch-heure.pipe';

describe('SwitchHeurePipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchHeurePipe();
    expect(pipe).toBeTruthy();
  });
});
